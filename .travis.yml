sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t adrianwitkowskistudy/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true adrianwitkowskistudy/react-test npm test

after_success:
  - docker build -t adrianwitkowskistudy/multi-client ./client
  - docker build -t adrianwitkowskistudy/multi-nginx ./nginx
  - docker build -t adrianwitkowskistudy/multi-server ./server
  - docker build -t adrianwitkowskistudy/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push adrianwitkowskistudy/multi-client
  - docker push adrianwitkowskistudy/multi-nginx
  - docker push adrianwitkowskistudy/multi-server
  - docker push adrianwitkowskistudy/multi-worker
